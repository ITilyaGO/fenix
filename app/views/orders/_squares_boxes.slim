- status ||= KSM::OrderStatus.find(order.id)
- @sections.sort_by(&:ix).each do |s|
  - s_st = order.order_parts.flatten.detect {|op| op.section_id == s.ix}
  / - if !s_st.nil? && s_st.transfer
  /   = content_tag :button, "П", :class => "btn btn-sm btn-#{s_st.nil? ? 'default' : s_st.state}", :style => "padding: 1px 9px;"
  / - else
  - color = status.state(s.ix)
  button.btn.btn-sm.btn-section.allow-print class=[("btn-#{color}"), ('with-digits' if s_st)]
    / btn- btn-#{s_st.nil? ? 'default' : s_st.state}"
    - if s_st
      .dgt.non-print = s_st&.boxes
    span.only-print = status.present?(s.ix) ? s.name[0..2] : ''
  | &nbsp;
| &nbsp;
